[package]
name = "bento-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }
tarpc = { version = "0.37.0", features = ["serde-transport-bincode"] }

[dev-dependencies]
serde_json = "1.0.149"

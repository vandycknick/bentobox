capabilities:
  dns:
    aliases:
      - host.docker.internal
  forward:
    enabled: true
    tcp:
      auto_discover: true
    uds:
      - name: docker
        guest_path: /var/run/docker.sock
        host_path: docker.sock
